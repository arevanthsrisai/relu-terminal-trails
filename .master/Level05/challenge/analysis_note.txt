The authentic credential was issued repeatedly while decoy credentials were
entered only once. Use terminal_history file to view the recovered commands, then use a
small `grep` pipeline to narrow the output to credential-looking lines.
uniq -q returns only the repeated values
Preserve the command stream; do not manually edit history.
